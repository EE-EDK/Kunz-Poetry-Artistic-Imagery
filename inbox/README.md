Drop new local artifacts here only if they are not yet tied to a poem.

Usual path: Ethan says **move / pull / retrieve / grab / file** → search
`C:\Users\edk7c\Downloads` (root, last ~10 minutes) → rename to
`NN-slug-brief-descriptor.ext` → place in `poems/NN-slug/<slot>/`. If the
poem is unclear, park the file here and ask. Do not leave production media
in this inbox once the poem is known.
